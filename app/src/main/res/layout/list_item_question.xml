<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable name="clickListener"
                  type="com.example.stackanswers.searchresults.SearchResultsAdapter.SearchResultsListener"/>
        <variable name="question"
                  type="com.example.stackanswers.network.Question"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/box_holder"
            android:textSize="24sp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="@{() -> clickListener.onClick(question)}">

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:layout_constraintEnd_toEndOf="parent" android:layout_marginEnd="8dp"
                app:layout_constraintStart_toStartOf="parent" android:layout_marginStart="8dp"
                android:layout_marginTop="8dp" app:layout_constraintTop_toTopOf="parent" android:id="@+id/question_title"
                android:textSize="18sp" android:textStyle="bold"
                android:text="@{question.title}"/>

        <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/question_score"
                app:layout_constraintTop_toBottomOf="@+id/question_title"
                app:layout_constraintEnd_toEndOf="parent" android:layout_marginEnd="8dp"
                app:layout_constraintStart_toStartOf="parent" android:layout_marginStart="8dp"
                android:layout_marginTop="24dp" android:textStyle="italic"
                android:text="@{question.score}" />

    </androidx.constraintlayout.widget.ConstraintLayout>


</layout>